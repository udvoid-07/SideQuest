# ── Supabase ──────────────────────────────────────────────
# Create project at supabase.com → Settings → API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ── Site URL ──────────────────────────────────────────────
# Used to build redirect links (e.g. password reset). Falls back to
# localhost in dev — only needs setting in production (Vercel env vars).
# Must also be added to Supabase → Authentication → URL Configuration
# → Redirect URLs, or Supabase will reject the redirect.
NEXT_PUBLIC_SITE_URL=https://side-quest-web.vercel.app

# ── Google API key (Gemini AI only) ──────────────────────
# console.cloud.google.com → Create project → Credentials → API Key
# Enable: "Generative Language API" (Gemini — quest search, personalization, week plan)
# Location data uses OpenStreetMap/Overpass API — free, no key needed
# NEVER prefix with NEXT_PUBLIC_ — server-side only
GOOGLE_API_KEY=your_google_api_key

# ── Mobile (Expo) ─────────────────────────────────────────
# Same Supabase values with EXPO_PUBLIC_ prefix for React Native
# Copy to apps/mobile/.env
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ── Web Push (VAPID) ──────────────────────────────────────
# Generate with: node -e "console.log(require('web-push').generateVAPIDKeys())"
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_SUBJECT=mailto:you@example.com

# Protects /api/cron/* routes. Vercel Cron sends this automatically as
# "Authorization: Bearer $CRON_SECRET" when set as a project env var.
CRON_SECRET=a_long_random_secret

# ── Optional ──────────────────────────────────────────────
# EXPO_ACCESS_TOKEN=   # Only needed for EAS Build / OTA updates
